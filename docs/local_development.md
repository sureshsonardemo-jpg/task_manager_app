# Local development

Use the Flutter version pinned by the project before fetching packages.

1. Run `flutter pub get`.
2. Start an Android emulator or iOS simulator.
3. Run `flutter analyze` before opening a pull request.

Keep local environment files out of version control.
